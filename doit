dir=$1
if [ "$dir" = '.' ]; then
 dir=`pwd`
fi
name=$(basename "$dir")
echo "************************************"
echo "**     building $name          **"
echo "************************************"
pushd $name
if [ -x build.sh ]; then
  build.sh
else
  sbt clean test package
fi

popd
